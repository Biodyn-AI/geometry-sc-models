# Developmental trajectories

Section 3.3. Pseudotime decoding, tangent rotation, cross-tissue transfer, endpoint targeting, and the expression-PCA controls.

16 scripts, 30 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `common.py` | Shared config for the control folder: the four tissue h5ad paths, terminal fate clusters and root clusters per tissue, the canonical marker panels, log1p-CP10k expression loading, Fisher combination. | none directly; carries the fate and marker definitions behind every §3.3 endpoint-purity and fate-diagonal num | nothing on its own |
| `baseline.py` | THE missing control: builds a 20-D whitened PCA of log1p-CP10k expression on the exact same cells and runs the identical rotation / steering / endpoint-purity pipeline against all four foundation mode | §3.3 — 'endpoint targeting on hard, low-headroom trajectories (mouse pancreas endpoint purity 0.207 → 0.569, w | cached .npz for all four models + the four tissue h5ad files (515 MB); ~4 min, n |
| `dump_shifts_maxtoki.py` | Dumps MaxToki-217M in-silico TF-activation embedding shifts (activation = move the gene to rank 1, the Geneformer-style analogue), statistics deliberately deferred to score_shifts.py. | Cross-model replication of the inversion negative (PERTURB_INVERT_CROSSMODEL.md). Not a §3.3 number. | MaxToki-217M safetensors (868 MB) + its adapter from projects/maxtoki/setup + Se |
| `dump_shifts_state.py` | Same dump for STATE-SE-600M (activation = set the gene's count to the cell's max), pushed through STATE's own dataloader in one pass. | Cross-model replication of the inversion negative. Not a §3.3 number. | STATE SE-600M weights (3.0 GB) + ESM2 protein embeddings + route_state/state_loa |
| `extract_scgpt_binned.py` | Re-extracts scGPT Setty/lung/gut/pancreas cell embeddings under scGPT's own 51-bin pretraining input convention instead of raw counts (changes exactly one thing vs the original extractor). | §5 'A note on extraction' — produces the corrected scgptbin_*.npz that every later number uses. The exact delt | needs the scGPT repo on sys.path, the whole-human checkpoint (205 MB), the tissu |
| `fate_matrix.py` | Breaks the p = 0.042 permutation floor on the fate diagonal by adding Setty's unused 5th branch (DCs) and transferring scGPT's gene-universal MVC head to three more tissues; adds a bootstrap CI on dia | Not quoted numerically in §3.3, but it is the evidence behind the paper's fate-steering framing: 14 of 15 targ | cached scgptbin .npz for 4 tissues + 4 h5ad + scGPT checkpoint; ~3 min, no GPU |
| `gene_decode.py` | Decodes each step of the steered path back to gene expression with a Ridge decoder fit on real train cells and asks whether the swept program is ordered, fate-specific hematopoiesis; also the library  | §3.3/§4.4 supporting evidence — stem program +0.20 → −0.53 (scGPT) under the steer vs +0.20 → +0.34 under a ra | cached .npz + data/hematopoiesis/setty19_cd34_bm.h5ad (170 MB); no GPU |
| `local_steering.py` | Ladder of steering rules of increasing locality (linear / quadratic / linear_proj / piecewise / local / local_proj / oracle_knn), scored by constrained advance at a fixed off-manifold budget; contains | §4.4 sec:operator — constrained advance 0.461 → 1.239, positive in 16 of 16 cells, beats the label-using oracl | cached embedding .npz only; seconds per tissue, no GPU |
| `lymphoid_ablation.py` | Tests whether the weak lymphoid branch was a thin 4-gene marker panel by swapping in 10 canonical B/T-lymphoid markers and changing nothing else. | Honest negative that the paper's caveat discipline rests on: D moves by +0.003, the caveat stands. | same as fate_matrix.py; ~40 s, no GPU |
| `maxtoki_native.py` | Second independent pretrained readout: pulls MaxToki-217M's untied lm_head (20275 x 1232) and final RMSNorm straight out of the safetensors file (raw header parse, no torch) and reruns the same gate a | Retires the 'scGPT-only readout' caveat: MaxToki passes the gate better than scGPT (+0.302 vs +0.260) and give | MaxToki-217M model.safetensors (868 MB, read by byte offset), token_dictionary.j |
| `native_decode.py` | Reruns the gene decode through scGPT's OWN pretrained MVC head (collapsed to a fixed matrix M with zero parameters fit on our data), removing decoder circularity; stage 1 is a readout gate on real cel | §5 'A note on extraction' — the gate FAILS on raw-count embeddings (within-cell Spearman +0.085) and PASSES on | cached .npz + Setty h5ad + scGPT whole-human checkpoint best_model.pt (205 MB) a |
| `perturb_invert.py` | In-silico activation of 604 TFs in scGPT (force to top expression bin), scores each shift's alignment with the on-manifold fate direction, and asks whether known lineage drivers beat a plain different | Not cited in §3.3. Supports the paper's navigation-vs-intervention dissociation (Discussion): AUROC 0.487–0.70 | scGPT checkpoint + Setty h5ad + DoRothEA/TRRUST union TSV; 38,656 forward passes |
| `robustness.py` | Turns the two knobs that could break the expression-PCA result — train/test split seed (5) and HVG count (1000/2000/5000) — on the constrained-advance comparison. | Supports §3.3's expression-PCA framing: expression beats scGPT 15/15, Geneformer 15/15, MaxToki 15/15, STATE 1 | same as baseline.py; ~3 min, no GPU |
| `score_shifts.py` | One scorer applied identically to every model's dumped shifts: fate-specific directions, AUROC of pre-registered driver sets, permutation p, BH-FDR, DE baseline and residual-on-DE. | Makes the three-model inversion comparison apples-to-apples. Not a §3.3 number. | the dumped shifts_*.npz (45–76 MB each) + Setty h5ad; no GPU |
| `st_fate_invert.py` | Tests STATE-ST, the perturbation-TRAINED model, on whether a CRISPRi knockdown shift points away from the right fate; includes a pre-registered validity gate on perturbation-specificity. | Closes the 'they are only embedding models' escape hatch: gate passes, yet GATA1 ranks 111/131 for anti-erythr | STATE-ST weights (542 MB) + STATE-SE encoder + cached setty_xstate.h5ad (95 MB)  |
| `tangent_diagnostic.py` | Core of the route: measures tangent rotation (theta_far, monotonicity) inside pseudotime quintiles against three nulls, and tests global linear-vs-quadratic steering for off-manifold drift; also the s | §3.3 — theta_far 48–113 deg between first and last pseudotime quintile, exceeding a heteroscedastic residual-p | cached embedding .npz only (data/branchpoint/{model}_{tissue}.npz + data/maxtoki |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
