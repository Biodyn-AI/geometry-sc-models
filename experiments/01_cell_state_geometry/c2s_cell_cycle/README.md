# The cell cycle in C2S-Scale

Section 3.1, 3.2, 4.2 and 5.1. The information accounting, the disk-versus-ring calibration, flatness, the angle/radius decomposition, continuity, the surface-form ladder, and the metric warp at the restriction point.

32 scripts, 40 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `c2s_gm_lib.py` | Thread A machinery: the order/circle/grid/antipodal statistics and their annotation-permutation nulls, copied verbatim from the internal source route, plus the coexpr and esm2 reference bases and the  | Thread A; supports 4.1 chromosome and 4.4 function/paralogy indirectly, not the four sections inventoried here | needs the raw Tabula Sapiens h5ads, an ESM2 protein-embedding .pt from the UCE-m |
| `cc_phase.py` | Tricycle/Revelio cell-cycle phase angle from marker-gene PCA, plus phase_angle_oriented which pins handedness and origin from marker biology (S precedes G2/M) so two datasets can be compared. | underpins every phase number in 3.1, 3.2, 4.2, 5.1; the orientation fix is what makes the 3.1 cross-cell-line  | an h5ad with gene-symbol var_names; nothing else |
| `cell_sentences.py` | Builds a C2S cell sentence from an h5ad and maps Gemma-2 subword tokens back to genes via offset_mapping. | the top-512-rank encoding that Table 1 in 3.1 is built around | tokenizer only, no model weights needed to build sentences |
| `ctx_lib_c2s.py` | Shared loaders and normalisation for the Thread B cube: balanced-count masking, per-dimension z-scoring, rank means. | Thread B | the ctx cube npz |
| `gene_sets.py` | The 17 hand-crafted geometric hypotheses (gene set + external non-expression coordinate + honest confound note) for the Thread A screen. | Thread A screen; the cellcycle_circle entry is carried as the positive control and fails its margin bootstrap | nothing |
| `steer_c2s.py` | Gemma-2 forward-hook steering harness: decoder-layer-site edits, model's-own-next-subword-logit readout, norm-relative doses, split-half propagation test. | the intervention machinery behind the 4.3 lineage-switch steering and the Thread A gate-3 runs | needs the model on GPU |
| `build_ctx_basis.py` | Builds the C2S model basis: per-gene mean residual at that gene's token positions over a Tabula Sapiens panel, streamed so memory is O(genes x layers x d_model). Produces bases/c2s_ctx_L*.npz. | Thread A infrastructure | needs the model on GPU plus the TS panel h5ad |
| `cc_benchmark_c2s.py` | Puts C2S on the existing matched-dimensionality cell-cycle benchmark (PCA-k, three probes, held out) against the expression baseline on the identical 3,000 K562 cells. | 3.1 'the models beat expression in 0 of 36 (C2S)'; supplies the top and bottom rows of Table 1 (expression 0.9 | cached activations plus the canonical k562 substrate; no GPU |
| `continuity_test.py` | Three continuity tests scored 0 (continuous) to 1 (discrete): occupancy density, metric profile against true phase difference, and behavioural interpolation read through the model's own marker logits; | 3.2 continuity scores 0.17 / 0.10 / 0.00; AND 5.1 the metric warp — knot-gap CV 0.318 (2B) and 0.295 (27B), la | needs the model on GPU for the interpolation arm; the other two arms need only c |
| `ctx_causal_c2s.py` | Thread B gate 5: is the functional-context axis causally used, via the split-half propagation test. | Thread B gate 5, passes | needs the model on GPU plus the ctx cube |
| `ctx_coexpr_null_c2s.py` | Thread B gate 4, the load-bearing one: is the functional-axis modulation beyond co-expression, scored as a residual above a power-versus-coherence curve fitted on synthetic module axes. | Thread B gate 4 FAILS at +2.4 sigma — the divergence from MaxToki's +8 sigma. This is the honest negative wort | the ctx cube plus the panel h5ad; no GPU |
| `ctx_extract_c2s.py` | Thread B extraction cube M[partition, context, gene, dim] with the occurrence cap, cell-level split-half, stored token rank and stopword filter baked in at extraction. | Thread B infrastructure | needs the model on GPU plus the cell-type-stratified panel |
| `ctx_functional_axes_c2s.py` | Thread B gate 3: do genes move along GO-defined functional axes, with a validity AUC and a random-partition null. | Thread B gate 3, z=+21.3 nuclear vs surface | the ctx cube plus a gene2go pickle; no GPU |
| `ctx_polysemy_c2s.py` | Thread B gate 1: is a gene's contextual shift gene-specific and reproducible across disjoint cell halves (the EXCESS metric). | Thread B gate 1, +0.916 at L09 | the ctx cube npz; no GPU |
| `ctx_position_c2s.py` | Thread B gate 2: is that gene-specific response just token-rank encoding? Residualises on rank and recomputes EXCESS. | Thread B gate 2, survives at +0.911 | the ctx cube npz; no GPU |
| `encoding_matched_transfer.py` | Frozen K562-to-RPE1 transfer across a ladder of expression encodings ending at exactly what the model receives (top-512, rank only), with a paired bootstrap over target cells. | 3.1 the model 0.789; and the spurious encoding-only-matched contrast delta +0.0153, CI [+0.0022, +0.0283], whi | two h5ads plus two cached activation dirs; no GPU |
| `lap_walk.py` | Iterative steering around the closed loop: fixed direction, fixed-projected, local-tangent and random arms, with geometric (unwrapped decoded phase) and behavioural (model's own marker logits) lap cou | 5.4 (sec:operator) 'behaviour follows geometry' — 1.579 behavioural laps for local-tangent against 0.212 for f | needs the model on GPU for the behavioural readout, plus fitted knots |
| `manifold_fit.py` | Fits and characterises the cell-state manifold: cross-validated circular decodability, ring score, radius CV, chord-deviation curvature, intrinsic dimension, per layer; writes the bin knots used by ev | 3.2 the loop lies in a fixed linear 2-plane; supplies the knots whose gap CV becomes the 5.1 result | a cached activations dir plus the substrate h5ad; no GPU |
| `manifold_steer.py` | Manifold-arc versus linear-chord versus norm-matched-random steering along the fitted knots, read through the model's own next-gene logits, with a two-part instrument gate (correlation AND dynamic ran | 5.4 the dose-sweep half of the representation-to-behaviour isometry (0.73 to 0.88 over three runs) | needs the model on GPU plus fitted knots |
| `manifold_steer_poles.py` | Same three steering arms but with a two-pole signed marker contrast readout, for ordered (non-cyclic) manifolds where the 12-bin softmax readout is pinned. | the differentiation/erythroid arm; supporting, not a numbered main-text claim | needs the model on GPU plus fitted knots for an ordered variable |
| `orthogonal_props.py` | Decomposes each cell-level covariate into what the phase angle explains versus what the radius explains, cross-validated. | 3.2 'Angle is phase; radius is cycling strength' — cycling strength R2 0.414 radial against 0.040 angular; pha | cached activations dir plus h5ad; no GPU |
| `plateau_test.py` | Activation-plateau test in the Shinkle et al. sense: relative output distance along an interpolation, stratified within-phase versus across a real transition, at three layers; plus a phase-resolved ou | 3.2 'no plateau, and a plateau statistic that weakens with depth (0.179 to 0.115 to 0.056 across layers 9, 15  | needs the model on GPU |
| `ring_analysis.py` | Single-cell ring-vs-filled-disk test against matched synthetic ring and disk calibrations, plus the cell-cycle-gene-deletion (compute vs relay) test. | 3.2 'It is a filled disk rather than a ring' — planted ring 0.15/0.000, planted disk 0.35/0.126, C2S-2B 0.38/0 | two cached activation dirs (normal sentences, and sentences with the 87 cc genes |
| `run_genome_wide.py` | Genome-wide chromosome classification from the C2S activation basis (22-class balanced accuracy), with HOX/protocadherin removed and a genomic-distance-decay arm. | the C2S counterpoint to 4.1 — best ctx layer 0.105 but ESM2 sequence decodes it better at 0.190, so not model- | bases/*.npz, both references, and species_chrom.csv; no GPU |
| `run_geometric.py` | Runs the 17-hypothesis geometric screen over every c2s_ctx layer against the coexpr and esm2 references, with the paired margin bootstrap over shared genes. | Thread A negative | bases/*.npz plus both reference bases; no GPU |
| `run_steering.py` | Thread A gate-3 causal steering: build the axis from the ctx basis, run the split-half propagation test per antipodal hypothesis. | Thread A gate 3 | needs the model on GPU plus bases and a panel |
| `subset_ts.py` | Builds the compact Tabula Sapiens panel h5ad on the same seed-0 cells the coexpr baseline uses, so the two describe identical cells. | panel construction | the raw Tabula Sapiens kidney/lung/immune h5ads at a hardcoded path |
| `subset_ts_celltype.py` | Builds the 12-cell-type x 1,000-cell stratified panel for Thread B. | panel construction | the raw Tabula Sapiens h5ads at a hardcoded path |
| `surface_forms.py` | The surface-form ladder: strip every cell-cycle gene from a real cell, insert one gene symbol at a fixed rank, read which pole of the real-cell phase circle the state lands on; five symbol forms ranke | the whole of 4.2 — CCNB1 0.989 (p=0.0002, 40.7 deg separation), Ccnb1 0.782, ccnb1 0.767, CQNB1 0.685, NBCC1 0 | needs the model on GPU, plus a substrate h5ad, a cached-states dir for the phase |
| `synth_sweep.py` | Constructive sufficiency test: build a phase-neutral backbone, insert an externally annotated peak-phase gene module at fixed ranks, and ask whether the representation rotates to that phase. Also expo | 114.3 deg +/- 6.9 against shuffled 13.2 and random 3.5. Not in the main-text sections listed; likely supplemen | needs the model on GPU |
| `transfer_test.py` | Zero-shot K562-to-RPE1 transfer with a measured constant/random floor, R_diff and rotation-corrected median error; includes the documented rejection of two candidate target datasets. | superseded by encoding_matched_transfer.py + selection_matched_arm.py; its results/transfer_test.json and tran | two h5ads plus two cached activation dirs; no GPU |
| `validate_local.py` | Zero-cost pre-flight: builds both reference bases locally, checks every hypothesis has enough genes, executes all four statistics and runs a baseline sanity check before any GPU time. | none — a smoke test, and a good one to keep as the repo's CI check | the two reference bases; no GPU |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
