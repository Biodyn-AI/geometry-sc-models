# UCE, STATE and cell-token geometry

Supporting arms for Sections 3 and 6. Representations whose gene tokens are frozen ESM-2 embeddings, which is what makes them the sequence control.

18 scripts, 11 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `common.py` | Shared helpers for the STATE-SE geometry tests: re-exports decode_phase / decode_phase_knn / loop_alignment from route_topology read-only, computes the Tirosh S+G2M phase angle from raw counts, builds | none directly; provides the phase coordinate recipe described in Supplement A.2 ('Tirosh S and G2M marker z-sc | route_topology/cellcycle_loop.py and route_geometry/build_rulers.py. Injects a f |
| `uce_loader.py` | Loads UCE-100M (4-layer, HF mirror minwoosun/uce-100m) via repos/UCE model.py, re-implements UCE's own sample_cell_sentences tokenizer and forward path, and exposes the layer-2 residual stream plus th | Supplement A.1 representations table, row 'UCE-100M ... transformer ... ESM-2 gene tokens ... L2'; A.1 note th | UCE repo pinned at 9c416007be15ad6753dc84af4468c1dc10421ab9; pytorch_model.bin ( |
| `cellcycle_state.py` | STATE-SE cell-cycle curvature test on K562, using per-cell mean-pooled L11 residuals streamed from the cached Route-B per-gene activations. | supports the STATE-SE arm of §3's 'the cell cycle is flat' but is NOT the source of the Supplement B.6 number  | data/state_activations/state_L11/ (part of a 9.4 GB tree) + replogle_k562_subset |
| `cellcycle_uce.py` | UCE cell-cycle curvature test on K562: Tirosh S/G2M phase angle vs linear/bilinear/kNN decode in a matched 20-D PCA space, with the mandatory synthetic linearly-embedded control. | not a cited number. Result: curvature -0.127 (control -0.045), curved=False. The paper's own cell-cycle anatom | data/cellcycle/uce_k562.npz + the K562 h5ad; imports route_state_geometry/common |
| `celltype_state.py` | STATE-SE cell-type test: kNN recovery vs shuffled-label null, curvature in a matched 20-D PCA space, and an argmax-linear synthetic control. | none cited; background for the 'cell-type identity is linearly accessible' line of §3 | the cached npz from ts_extract.py + route_lineage/lineage_manifold.py. ALSO a li |
| `celltype_uce.py` | UCE cell-type test on Tabula Sapiens immune: kNN balanced-accuracy recovery vs shuffled-label null, plus classification curvature and an argmax-linear synthetic control. | not a cited number. Result: bal-acc 0.433 vs null 0.031, curvature +0.011 against a synthetic control at -0.31 | data/celltype/uce_ts_immune.npz; imports route_lineage/lineage_manifold.py and r |
| `compare_celltoken.py` | The cross-model cell-token battery: for each model x representation (gene-mean-pool, cell token at the tap layer, cell token at the final layer) computes TwoNN intrinsic dimension, participation ratio | no cited number. Its finding retracts a cross-model reading of route_uce/RESULTS.md: UCE's cell token is 3rd o | the celltoken npz files + route_branchpoint/branchpoint_geometry.py and route_uc |
| `extract_branchpoint.py` | Per-cell mean-pooled layer-2 UCE residual (+ CLS residual + final X_uce) on the four Setty-schema differentiation trajectories, written to data/branchpoint/uce_{setty,lung,gut,pancreas}.npz. | Supplies the UCE column of Supplement B.2 'Developmental pseudotime, 0 of 20' (blood -0.139, lung -0.104, gut  | UCE checkpoint + the four trajectory h5ads (Setty CD34+, lung airway, fetal gut, |
| `extract_geneformer_cls.py` | Extracts Geneformer V2-316M's [CLS] token at the tap layer 11 and the final layer 17, plus the gene mean-pool at layer 11, from one forward pass. | none cited | ctheodoris/Geneformer V2-316M from HF, three gene/token pickle dictionaries from |
| `extract_maxtoki_cls.py` | Extracts MaxToki-217M's <eos> token (the only causal position that sees the whole cell) at hidden_states[8] and the final layer, plus the gene mean-pool. | none — this arm produced no data | MaxToki 217M via an adapter in projects/maxtoki/setup, a gene-name→Ensembl pickl |
| `extract_percell.py` | Generic per-cell UCE extractor driven by env vars (any h5ad, any obs columns to carry) — used for Replogle K562 cell cycle and Tabula Sapiens immune cell type. | Supplies the UCE cells of Supplement B.2 'Five prior models, 1 of 45' — specifically 'UCE loses all 9 of its c | UCE checkpoint + a source h5ad. Writes data/cellcycle/uce_k562.npz (3,000 cells) |
| `extract_regulatory.py` | Per-gene-token layer-2 UCE residual on 1,200 Replogle K562 cells (unique sampled genes only) → 1,008,196 tokens over 6,369 genes, in route_state's storage layout. | none in the current paper — this is the Route-B regulatory-logic substrate, background rather than a cited num | UCE checkpoint + data/state_activations/replogle_k562_subset.h5ad. Writes a 2.58 |
| `extract_scgpt_cls.py` | Extracts scGPT's native <cls> cell token (vocab id 60695, value 0) at L11 together with the gene mean-pool from one forward pass, under scGPT's own 51-bin quantile-binned input convention. | its docstring and route_celltoken/RESULTS.md pitfall 1 document the defect quantified in Supplement A.3 'Input | scGPT whole-human checkpoint + vocab.json from a private external tree, and the  |
| `extract_state_cls.py` | Extracts STATE SE-600M's CLS token at tap layer 11 and final layer 15 plus the gene mean-pool, from one forward pass. | none — this arm produced no data | STATE SE-600M via route_state/state_loader.py + the Setty h5ad. NEVER COMPLETED: |
| `manifold_celltoken.py` | Experiment 1+2 — intrinsic geometry (TwoNN ID, PCA participation ratio, dim@90% variance) and trajectory curvature plus tangent rotation for three UCE representations side by side (gene-mean-pool, lay | no cited number, but it is the source of the d90=201 censoring sentinel caveat (only 200 PCs are computed, lin | all UCE npz files (data/branchpoint, data/xuce, data/cellcycle, data/celltype).  |
| `train_compare.py` | Trains a linear TopK SAE (PolySAE degree 1) and a bilinear autoencoder (degree 2) at matched budget on the UCE layer-2 activations, then runs the pre-registered R2_x / R2_SAE / novel-nonlinear compari | none in the current paper. Result: fidelity gate PASS (0.826 / 0.864), redundancy 81.1%, genuinely nonlinear 2 | the 2.4 GB activation cache from extract_regulatory.py, plus route_b/data.py and |
| `ts_extract.py` | Fresh forward pass of Tabula Sapiens immune/kidney/lung cells through STATE-SE with a hook on transformer_encoder.layers[11], mean-pooling gene positions (skipping CLS) to one 2048-vector per cell. | Supplement A.1 STATE-SE row, 'L11, skipping CLS' | STATE SE-600M weights via route_state/state_loader.py, STATE's own Inference + c |
| `universal_manifold.py` | Experiment 3 — pools all six datasets in the UCE cell-token space and tests (A) kNN dataset purity, (B) leave-one-tissue-out pseudotime transfer raw and per-tissue-centered, (C) cross-species integrat | no cited number; independently reproduces the direction of Supplement B.4, 'Transfer largely fails for every r | all UCE npz files. Result: kNN dataset purity 0.999-1.000; within-tissue R2 0.69 |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
