# The steering operator ladder

Section 5.4. Linear, quadratic, local, projection and retraction, against an oracle allowed held-out labels.

14 scripts, 8 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `_data.py` | Minimal h5py/scipy loaders for the two benchmarks (Setty log1p CP10k counts; LARRY 40-PC space + clone matrix). Deliberately separate so manifold_steer.py has no data dependency. | none (data plumbing) | Setty h5ad, scgptbin_setty.npz, LARRY h5ad — all by hardcoded absolute path |
| `geom_common.py` | Shared ladder harness: npz loading with the CAP subsample and fixed seed, run_ladder() over a rule bank (linear / linear_proj / local / local_proj / oracle_knn / linear_retract / local_retract), and t | supports §5.4 ladder rows via gate0/gate2; the retraction fields are the new code here | needs codebase/route_steering/{tangent_diagnostic,local_steering}.py on sys.path |
| `manifold_steer.py` | The released tool: ManifoldSteer class — whitened-PCA manifold, local-tangent basis (top-m PCs of k=100 nearest real cells), tangent projection, ascent/fate directions, on-manifold integrator, off-man | §5.4 (sec:operator): 'Local direction + tangent projection + retraction ... The operator needs only a cell-by- | nothing — numpy + scikit-learn only, zero data dependency, zero project imports |
| `benchmark_larry.py` | Cross-dataset validation on Weinreb LARRY: steer undifferentiated cells at each of 6 real terminal fates, score by nearest-terminal-cell vote at the endpoint, compare projected vs plain-linear path pl | §5.4 supporting evidence for the released tool (numbers not quoted in main.tex). Produces 6/6 diagonal hits bo | LARRY h5ad (88 MB) via manifold_steer/_data.py |
| `benchmark_setty.py` | Ablation ladder using only ManifoldSteer on model-free Setty counts: linear / linear_proj / retract / random_proj / oracle, scored by constrained advance CA(tau) with an independent kNN judge. | §5.4 model-free claim; supplement §D.6 ladder in counts. Produces ca13 linear 1.916, linear_proj 2.187, retrac | Setty h5ad (170 MB) + scgptbin_setty.npz (12 MB, only for pseudotime/cell_idx/cl |
| `example.py` | Self-contained synthetic demo: bent 2-branch toy manifold, shows projected steering keeps off-manifold ratio ~1 while plain-linear cuts the corner and climbs. | none (documentation/demo for §5.4's released tool) | nothing — pure synthetic, no data, no model |
| `gate0_counts_vs_model.py` | Gate 0 — runs the identical steering ladder in the model's 20-PC whitened embedding space and in the 20-PC whitened space of raw log1p CP10k counts over the SAME cells, and compares the projection gai | §5.4 final sentence ('needs only a cell-by-gene matrix ... model-free tool'). Produces projection gain MODEL + | 4 cached embedding npz (scgptbin/geneformer/state/maxtoki _setty.npz, 12-29 MB e |
| `gate1_within_branch_rotation.py` | Gate 1 — recomputes the theta_far tangent-rotation statistic and its calibrated null restricted to a single non-branching lineage, to separate maturation curvature from branch fan-out. | Not §5.4. It is the premise check under §5.5 (sec:curv, 'the geometry arrives bent') and §5.4's 'the tree shap | 4 cached embedding npz only (no h5ad, no checkpoint); imports route_steering/tan |
| `gate2_retraction.py` | Gate 2 — adds linear_retract and local_retract (step along w, then pull to the mean of the 30 nearest real cells) to the ladder, to test whether tangent projection is more than graph-walking. | §5.4 'Retraction cures outward drift but never the stall'; supplement §D.5/§D.6 retraction rows. Produces proj | same 4 embedding npz + Setty h5ad (counts arm for scgptbin only); imports route_ |
| `ha2_onmanifold_perturb.py` | H-A2 — decompose each cached in-silico perturbation shift into tangent + normal at the source cell, score only the tangent component against fate directions, and use the normal fraction as a plausibil | supplement §F.4 'Perturbation inversion' — this hardens that negative. Produces maxtoki raw 0.475 / tangent 0. | maxtoki_setty.npz + state_setty.npz + shifts_maxtoki_setty.npz (47 MB) + shifts_ |
| `ha3_heldout_population.py` | H-A3 — delete the Ery_1 intermediate from the manifold, steer progenitors at Ery_2 on-manifold, decode the path with scGPT's own MVC head, and ask whether the path passes through the held-out transcri | supplement §F.4 'Reconstructing a deleted intermediate. Held-out peak r = +0.545, rank 5/10, path running thro | scgptbin_setty.npz + Setty h5ad + scGPT checkpoint best_model.pt (205 MB) + torc |
| `hb1_larry_curvature.py` | H-B1 — does local manifold geometry at an undifferentiated LARRY cell predict its clone's neutrophil-vs-monocyte fate beyond the cell's own 40-PC expression position? Clone-split GroupKFold CV, positi | supplement §F.4 'Hidden clonal fate (LARRY)': expression position AUROC 0.767, geometry 0.540 vs clone-permuta | LARRY h5ad (88 MB) read directly with h5py; imports only auroc from route_steeri |
| `hb2_normal_space.py` | H-B2 — tangent co-movement cos(V.MR[a], V.MR[b]) at branch-point progenitors through scGPT's pretrained MVC head, testing whether known antagonistic TF pairs are more opposed than cooperative ones, wi | none — not cited in main.tex or the supplement. Internal negative (labelled AUROC 0.90 local tangent vs 1.00 f | scgptbin_setty.npz + Setty h5ad + scGPT checkpoint best_model.pt + torch; import |
| `hb3_tangent_dimension.py` | H-B3 — local tangent dimension as participation ratio of the local covariance spectrum (k=100): tests potency correlation with pseudotime, multipotent-vs-terminal Cohen's d, and whether the decline is | supplement §F.4 'Commitment as a discrete geometric step. No cliff (sigmoid transition width 0.27-0.83 of the  | 4 cached embedding npz + Setty h5ad (counts arm); imports route_steering/gene_de |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
