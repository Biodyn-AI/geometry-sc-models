# Curvature: shape against size

Section 5.5. Angular versus radial curvature at matched width across all layers.

18 scripts, 34 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `arms.py` | Splits Q into positive and negative semidefinite arms (Q = Q₊ − Q₋) and scores full/plus/minus out-of-fold; also exports split_arms() and qf() used elsewhere. | The 'irreducible saddle' reading — later falsified. Related to supplement F.1 'The GATA1↔PU.1 saddle is a mutu | data .npz |
| `qfit.py` | Core estimator: closed-form-profiled optimal-ridge linear baseline plus a low-rank symmetric (possibly indefinite) Q fitted on the deflated target, with the nested-CV harness and both null generators. | §5.5 / Table 4 (tab:curv) — imported UNCHANGED by route_curvature_mech/curvature_mech.py, which is what actual | a .npz of {emb, pseudotime} per (model, dataset) under data/branchpoint; no mode |
| `abundance_control.py` | Partials per-cell log total-counts and the raw-mean abundance axis out of both embeddings, then re-scores cell cycle over 10 seeds. | SUPERSEDED. Reports the margin tripling (S: +0.058 → +0.186; G2M: +0.067 → +0.139). Caveat recorded in GEOMETR | results/emb_cache/geneformer_L11.npz (74 MB) plus a re-read of replogle_concat.h |
| `arms_identifiability.py` | The control that falsified the saddle reading: re-fits Q at six (r, λ) settings with indistinguishable held-out Δ_Q and shows the arm split flips sign. | At (r=8, λ=1e-3) both arms are harmful (−0.0568/−0.0681); at (r=32, λ=1e-2) both are useful (+0.0162/+0.0123)  | data .npz; imports qfit and arms |
| `build_rulers.py` | Builds the biological label vectors: scGPT cell-type/tissue from Tabula Sapiens extraction metadata, Geneformer Tirosh S/G2M cell-cycle scores from Replogle K562 non-targeting controls. | none. CONTAINS A KNOWN BUG at lines 91-92: replogle_concat.h5ad X is already log1p(CP10k), and this applies lo | replogle_concat.h5ad and a Tabula Sapiens extraction_metadata.json, both on exte |
| `discriminator.py` | The pre-registered redundancy discriminator: out-of-sample R² predicting each Q latent from raw x (R²_x) and from linear-atlas features (R²_SAE), with prereg thresholds 0.25 / 0.50. | R²_SAE = 0.79 for the saddle latent, above the 0.50 novelty threshold ⇒ fully redundant with the linear atlas. | data .npz plus results/atlas_feats_scgpt_setty.npz (107 MB) |
| `extract_atlas_feats.py` | Encodes each cell's gene TOKENS with the linear TopK SAE atlas and mean-pools the feature activations (h_bar), the in-distribution baseline; also the naive h_hat for continuity. | The domain-shift guard that stops a crippled atlas from inflating the novelty claim. Method note only. | scGPT whole-human checkpoint + vocab, the bio-sae TopKSAE source, the trained la |
| `feat_identifiability.py` | Identifiability check in feature space: Jaccard overlap of top-30 interacting feature pairs and top eigen-feature sets across six (r, λ) configs and two seeds. | Pair Jaccard 0.43–0.50 vs the reference config; eigen-feature Jaccard 0.94–1.00. Named products are only partl | atlas_feats npz plus qfit |
| `geometry_test.py` | Tier-A test: mean-pools four matched per-cell representations (raw_mean, SVD-50, linear TopK SAE, bilinear atomic latents) from L11 tokens and scores kNN ruler recovery against a shuffled-label null. | SUPERSEDED. Its positive (bilinear 0.643/0.724 S/G2M R², 0.394 cell-type bal-acc vs linear 0.586/0.660/0.371)  | 17 GB of cached L11 token activations plus trained AEs under route_b/runs (a sib |
| `make_figure.py` | Four-panel summary figure: rank sweep, nulls, arm split, redundancy discriminator. | figures/route_q_summary.png — an internal figure, NOT paper Figure 7. Paper Figure 7 is produced by manifolds/ | the route's own JSON results only |
| `program_stability.py` | The control that falsified the gene-level read: scores four a-priori gene sets by the saddle score across six (r, λ) fits, asking only whether the sign is stable. | Only a coarse program-level read survives; the gene list does not (top-15 overlap 1/15). RESULTS.md §2.3. | data .npz plus results/gene_centroids_scgpt.npz (40 MB cache) |
| `run_features.py` | Refits the same Q on the NAMED atlas feature space h_bar instead of raw residual directions, so interactions read as 'feature A × feature B'. | Feature-space Δ_Q = +0.0293, CI [+0.0262,+0.0327], R²_lin(atlas) 0.8731 (feat_q.log). Undocumented in RESULTS. | results/atlas_feats_scgpt_setty.npz (107 MB) plus the atlas annotation JSONs |
| `run_generality.sh` | Shell driver: 4 models × 3 human lineages in quick mode plus mouse pancreas in full mode. | The 16-cell generality table in GENERALITY.md; the pre-registered mouse-pancreas negative control FAILS (4/4 p | all 16 data/branchpoint .npz files; hardcodes ../../.venv/bin/python |
| `run_nulls.py` | Runs the two pre-registered null distributions (label-shuffle and linear-synthetic), refitting the whole nested pipeline per surrogate. | Both nulls pass: shuffle max −0.0024, linear-synthetic max −0.0003 against real +0.0250 (~60 sd). Supports pap | same .npz plus the q_<model>_<ds>.json/.npz from run_substrate.py |
| `run_substrate.py` | Headline entrypoint for one (model, dataset): nested-CV Δ_Q, bootstrap CI, rank sweep, abundance-off variant. | Δ_Q = +0.0250, CI [+0.0221,+0.0282], R²_lin 0.8945 → R²_quad 0.9195 on scGPT/Setty (RESULTS.md §1). Not quoted | data/branchpoint/<model>_<dataset>.npz |
| `spectrum.py` | Out-of-fold eigen-truncation curve at r=32: how much held-out curvature survives keeping only the top-j eigen-directions. | Shows eigen-ordered truncation is not a valid decomposition here (methodological catch, RESULTS.md §2.1). | data .npz plus q_<m>_<ds>.json |
| `structure_geometry.py` | Multi-seed (10 CV seeds) robustness on the bilinear-minus-linear margin, plus the structure sweep (atomic/composite/block_diag/nnz8/sink) re-scored on the geometry ruler. | SUPERSEDED, same as geometry_test.py. Produces margins +0.058±0.002 (S), +0.067±0.004 (G2M), +0.024±0.006 (cel | same 17 GB activations plus six trained AE run dirs; writes the 212 MB emb_cache |
| `summarize.py` | Assembles GENERALITY.md (4 models × 4 datasets) and cross-checks each Δ_Q against the published kNN/kernel curvature from route_branchpoint. | Produces the Δ_kernel column — the ONLY place either assigned path touches the deprecated kNN-minus-linear fam | the 16 q_*.json plus route_branchpoint/results/full_space_curvature.json |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
