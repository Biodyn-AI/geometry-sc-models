# Chromosome as a vocabulary-level variable

Section 4.1. Genome-wide decoding, the 10-megabase neighbourhood holdout, the tokeniser-ID deleak, the factorisation baseline, causal steering through the model's own head, karyotype validation, HOX, and the attention test that came out negative.

97 scripts, 130 committed result files.

Every script here needs assets this repository does not ship. See [`docs/DATA.md`](../../../docs/DATA.md) for what to download and where to point `GEOMSC_DATA` and `GEOMSC_MODELS`.

| script | what it computes | supports | needs |
|---|---|---|---|
| `gene_sets.py` | Hand-written registry of the 17 early geometric hypotheses (HOX grid, cell-cycle circle, secretory order, ...) with their external coordinates. | none (pre-§4.1 exploration) | nothing |
| `gm_lib.py` | Shared library: loads every gene representation basis (maxtoki_we, maxtoki_lmhead, geneformer_we, geneformer_v1_we, scgpt_we, esm2, coexpr, coexpr_devel, coexpr_k562, scGPT contextual ctx_L*), builds  | underpins every number in §4.1 | model safetensors (MaxToki-217M, Geneformer V1/V2, scGPT), ESM-2 .pt from the UC |
| `h_tokenizer_rank.py` | Calibration control: a gene's raw MaxToki token-ID integer as a decodable-but-meaningless label. | none (screen loader) | model tables + token dictionary |
| `hypotheses.py` | Hand-written registry of 40+ gene-property hypotheses (symbol -> label, kind, provenance, expectation) for the steerable-directions screen. | none (screening program) | various annotation files at hardcoded external paths |
| `steer_lib.py` | Shared activation-steering harness for MaxToki: Direction objects with provenance tags, hooks to inject at embed_tokens or after any decoder layer, logits/hidden readouts, both checkpoints. | §3.4 protocol for all of §4.1's causal results | MaxToki-217M / 1B HF safetensors via projects/maxtoki/setup/maxtoki_adapter.py |
| `antipodal_subspace.py` | Re-tests the antagonistic-TF antipodal hypothesis inside a marker-defined subspace instead of full space (global-PC ablation, lineage-subspace cosine, marker-axis projection). | §4.3 'GATA1/SPI1 separation 0.818, subspace cosine -0.394 model vs +0.007 co-expression vs +0.699 ESM-2' | model tables + esm2 + co-expression |
| `causal_ablate.py` | Causal ablation of the secretory axis - a well-powered negative (n=14,696) used as the control case for what a used axis should look like. | RESULTS.md §6 negative | MaxToki forward passes + Setty h5ad |
| `cellcycle_geometry.py` | Diagnostic for the steering negative: the gene-level cell-cycle geometry is an axis, not a circle. | RESULTS.md §14 | model tables + phase annotation |
| `cellcycle_steer.py` | Can you move around the gene-level cell-cycle circle by steering (no phase tracking at any dose). | RESULTS.md §14 negative | MaxToki forward passes |
| `chrom_cnv_origin.py` | Did the model learn chromosome from copy-number variation: contrasts aneuploid K562 against normal panels as the source of chromosome-scale co-expression. | Supp S5 DNA-dosage alternative | K562 + normal h5ad panels |
| `classifier_sweep.py` | Linear probe-family sweep (logistic C, LDA-Ledoit-Wolf, LinearSVC, whitening) for the 22-class chromosome task, per model. | §3.2 'each given its own best probe' | model tables + coordinates |
| `cnv_gene_level_control.py` | Kills the domain-shift alternative explanation for the gene-level fingerprint result. | Supp S5 control | K562 + normal panels |
| `cnv_gene_level_test.py` | Per-gene chromosome-placement fingerprint: does it resemble the normal panel or the aneuploid panel (within-chromosome contrast -0.091, CI [-0.152,-0.033]). | Supp S5 'its per-gene success pattern tracks the normal panel over the aneuploid one' | K562 + normal panels + model tables |
| `coexpr_fact.py` | Builds the PPMI-weighted truncated-SVD co-occurrence bases for every panel (the correct null the Stage-1 screen never used) and self-tests them. | §3.2 baseline construction for §4.1 | h5ad panels; writes bases into the data cache |
| `coocc_bestprobe.py` | Best-probe-vs-best-probe grid (logistic / LinearSVC / MLP) for the two models and the shallow LSA baseline. | §4.1 feeds the 0.880 / 0.720 / 0.506 numbers | cached panels + model tables |
| `coocc_diagnose.py` | Diagnoses why fetal gut factorises to 0.551 while Tabula Sapiens sits at 0.081 (kills preprocessing, file-order, frequency and single-chromosome explanations). | Supp S4 panel-dependence of the co-occurrence baseline | expression panels |
| `coocc_fair.py` | Width-matched and corpus-size-swept version of the shallow baseline (8k / 25k / 62,849 cells). | supporting; its width-matched block is explicitly retracted inside coocc_final.py | expression panels |
| `coocc_final.py` | Full-corpus shallow factorisation at its best dimension vs both models at native width, plus a PCA compression-bias control. | §4.1 factorisation baseline 0.692 group-split at LSA-256 | 62,849-cell fetal gut panel + model tables |
| `coocc_matched.py` | Matched-gene verdict table: raw expression profile (0.044) vs LSA-64/256/1024 of the identical cells vs both models, plus cell-shuffle and label-shuffle nulls. | §4.1 'raw expression profile 0.044' and 'permuted labels 0.045' | expression panels + model tables |
| `coocc_position.py` | Same factorised baseline applied to the within-chromosome position claim. | §4.1 last sentence / Supp S2: factorisation beats both models on position (rho +0.762 vs +0.622 and +0.374) | expression panels + model tables |
| `coocc_strongest.py` | The strongest training-free co-occurrence factorisation baseline (PPMI/LSA over top-2048-gene documents, panel and dimension swept) vs both models, matched genes, each representation given its own bes | §4.1 / Fig 3c headline: MaxToki-1B 0.880, strongest factorisation 0.720, MaxToki-217M 0.506, chance 0.045, n=1 | expression panels (fetal gut 62,849 cells, Tabula Sapiens) + both model tables |
| `corpus_s1_audit.py` | Audits MaxToki's Genecorpus-175M constituent studies (from the published supplementary xlsx) for tumour / cell-line content. | Supp S5 'only ~5-6% of its cells come from studies that sampled cancer' | a third-party supplementary spreadsheet hardcoded to ~/Downloads/media-1.xlsx |
| `ctx_anisotropy.py` | Anisotropy and self-similarity metric-validity control (Ethayarajh 2019). | none in §4.1 | ctx npz |
| `ctx_causal.py` | Is the functional-context direction causally used (push some genes along the axis, read others). | none in §4.1 | MaxToki forward passes |
| `ctx_celltype_loadings.py` | Saves per-cell-type projections of the context main effect onto the nuclear/surface axis so quoted loadings are traceable. | none in §4.1 | ctx npz |
| `ctx_coexpr_null_v2.py` | Definitive co-expression null for the functional-axis modulation, resolving the +1.7 sigma vs +8 sigma discrepancy. | none in §4.1 (relevant to §4.3's function-beyond-co-expression logic) | ctx npz + expression panels |
| `ctx_cross_model.py` | Cross-model / scaling comparison of the contextualisation phenomenon. | none in §4.1 | ctx npz for all models |
| `ctx_curated_targets.py` | Do TFs sit near their ChIP/literature targets beyond co-expression, and more where the target programme is active. | none in §4.1 | ctx npz + curated TF-target tables |
| `ctx_devel_trajectory.py` | Functional-axis trajectory across hematopoietic differentiation. | none in §4.1 | ctx_devel npz |
| `ctx_directional_probe.py` | Level-2 feasibility: does a gene move toward its own functional pole in contexts sharing that function. | none in §4.1 | ctx npz |
| `ctx_extract_devel.py` | Same extraction across hematopoietic differentiation branches (Setty CD34). | none in §4.1 | MaxToki forward passes + Setty h5ad |
| `ctx_extract_maxtoki.py` | Extracts MaxToki per-gene contextual representations stratified by cell type, with a pairwise noise control. Writes the 1.2 GB ctx_maxtoki_L*.npz. | none in §4.1 - belongs to the separate gene-context paper | MaxToki forward passes over Tabula Sapiens; writes very large npz |
| `ctx_extract_random.py` | Random-weights control extraction for the contextualisation phenomenon. | none in §4.1 | randomly initialised MaxToki |
| `ctx_extract_scgpt.py` | Reformats the cached scGPT contextual atlas into the shared ctx schema. | none in §4.1 | external scGPT activation atlas (subproject_42) |
| `ctx_extract_state.py` | Extracts STATE-SE (Arc SE-600M) per-gene contextual representations in the same schema. | none in §4.1 | STATE-SE weights + a 4 GB local cache |
| `ctx_feasibility.py` | Pre-extraction feasibility: gene counts and balance, and the housekeeping-stopword trap check. | none in §4.1 | tokenizer + h5ad only |
| `ctx_figures.py` | Builds figures/ctx_fig1..6.pdf for the gene-context paper. | none in §4.1 | results/ctx_*.json |
| `ctx_functional_axes.py` | Do genes move along functionally meaningful directions (nuclear/surface, mito/cyto, transcription/transport). | none in §4.1 | ctx npz + GO sets |
| `ctx_independent.py` | Independent-dataset replication of the Level-1 context claims on Setty CD34. | none in §4.1 | ctx_devel npz |
| `ctx_layer_curve.py` | Full 12-layer depth profile of the gene-specific context response. | none in §4.1 | ctxscan_L00..L11 npz (2.4 GB) |
| `ctx_polysemy.py` | Is a gene's contextual shift gene-specific rather than crowd drift (EXCESS +0.758 at L4, 0.000 at L0). | none in §4.1 | ctx_*_L*.npz |
| `ctx_position_confound.py` | Is the gene-specific context response just rank-position encoding. | none in §4.1 | ctx npz |
| `ctx_prediction_link.py` | Does contextualisation serve the model's next-gene objective. | none in §4.1 | MaxToki forward passes |
| `ctx_switcher_test.py` | Do documented context-switching TFs move more across contexts than matched controls. | none in §4.1 | ctx npz |
| `ctx_tightness_null.py` | Representation-compactness null for the same modulation. | none in §4.1 | ctx npz |
| `final_probe_grid.py` | Best linear vs best non-linear probe on both models AND both baselines on matched genes, to check the beat-both margin survives at higher probe capacity. | §4.1 probe-capacity control | all bases + coordinates |
| `genome_causal.py` | THE causal steering test: build the chr-C direction in embed_tokens space, add it to a random half of a cell's gene tokens, read chr-C mass in the next-gene logits at the other half; norm-matched rand | §4.1 causal use: +0.055, bootstrap CI [+0.013, +0.108], 18/22 chromosomes positive, random push -0.0004 | MaxToki-217M forward passes (transformers venv) + Setty CD34 h5ad |
| `genome_causal_sweep.py` | Robustness for genome_causal.py: alpha sweep x 3 seeds, dose-response, sub-natural alpha=0.5. | §4.1 'genuine dose-response, significant across three seeds even at a sub-natural strength'; Fig 2a of the rou | MaxToki-217M forward passes |
| `genome_deleak.py` | The tokeniser-ID deleak done honestly: kNN on token_id alone (0.489 at k=1), whole-1000-token accession-block CV, and the Geneformer parity dissociation. | §4.1 'holding out whole token-ID blocks retains 96%' (0.4332 -> 0.4145) | gm_lib bases + token dictionaries |
| `genome_groupsplit.py` | The 10-Mb genomic-neighbourhood holdout (GroupKFold over 10-Mb blocks), identical folds for every representation. | §4.1 / Fig 3a: MaxToki lm_head 0.433->0.347 (78% retained), W_E 0.433->0.373 (85%), Geneformer 51%, scGPT 24%, | gm_lib bases + species_chrom.csv |
| `genome_intersection.py` | Re-scores the same 22-way probe on the gene set common to all bases (n=14,671) so the beat-both comparison is not a coverage artefact. | §4.1 supporting; explains why ESM-2's own-set 0.19 drops to 0.105 on shared genes | gm_lib bases + species_chrom.csv |
| `genome_position2.py` | Within-chromosome fine position, artefact-corrected: signed Spearman with a per-basis within-chromosome position-shuffle null subtracted. | Supp S2: MaxToki excess +0.396 (22/22 chromosomes) vs ESM-2 +0.253 vs co-expression +0.063 | all bases + coordinates |
| `genome_position_direction.py` | Is position a linear direction, non-linear, or curved: dimensionality and alignment of the ridge weight vector, per chromosome. | Supp S2 geometry | MaxToki lm_head + coordinates |
| `genome_position_geometry.py` | How position is encoded: per-gene leakage-clean linear evidence, chromosome-specific gradients, orthogonality to top PCs. Writes the one large (1.3 MB) result file. | Supp S2 'a chromosome-specific linear gradient' | MaxToki tables + coordinates |
| `genome_seq_mechanism.py` | Why ESM-2 predicts chromosome at all: nearest protein neighbour is same-chromosome 22.3% vs 5.6% chance, and only 7.7% for HOX. | §4.1 'its signal was tandem-duplicate family resemblance' | esm2 basis + coordinates |
| `genome_wide.py` | THE genome-wide 22-way chromosome probe on every basis, on each basis's own gene set, with and without HOX+protocadherin removed. | §4.1 decoding row: MaxToki 0.43, Geneformer 0.17, scGPT 0.09, ESM-2 0.19, raw co-expression ~0.05, chance 0.04 | gm_lib bases + species_chrom.csv coordinate table |
| `hox_analogy_null.py` | The strict shuffled-grid null for the word2vec analogy that hox_analogy.py promised and omitted, plus the per-cluster breakdown. | §4.3 'HOXA9 - HOXA1 + HOXB1 lands on HOXB9 at 3.95x a strict within-cluster null (z=+13.5)' | model tables + esm2 + co-expression |
| `hox_axes.py` | Decomposes the HOX analogy into a paralog offset and a cluster offset and measures how parallel-transportable each is. | route paper Supp S7 'approximately a bilinear grid, not a rigid one' (R^2 = 0.53) | model tables |
| `hox_causal_locus.py` | Observational causal test at the HOX locus: regress the model's own logit for an absent gene on whether other genes from its cluster are in context, controlling for gene identity, cell HOX-friendlines | §4.3 of the route paper: beta = +0.093 (t=7.9) fetal gut, +0.146 (t=6.1) aging marrow; HOXB +0.26/+0.25, HOXA  | MaxToki forward passes + fetal-gut and aging-marrow h5ad (row cache in results/c |
| `hox_crossmodel.py` | The HOX 4-class cluster task across the model portfolio, adding the Geneformer V1/V2 loaders. | RESULTS.md §11: Geneformer 0.81, scGPT 0.57; UCE/STATE cannot be asked | all model tables |
| `hox_within.py` | Within-cluster paralog: leave-one-cluster-out transfer of the paralog coordinate, and the PC2 correction to hox_shape.py. | §4.3 'the paralog coordinate transfers to it at held-out rho=+0.833, correct sign in 4 of 4 clusters (p=0.005) | model tables + co-expression panels |
| `infercnv_confound.py` | Does cell-type composition manufacture spurious CNV calls in karyotypically normal cells (and the diverse-reference mitigation). | an applied side result, not cited in main §4.1 | h5ad panels |
| `karyotype_sim.py` | Karyotype validation: derive a per-chromosome shift from aneuploid K562 minus lineage-matched CD34+, steer all 22 chromosomes at once, correlate the model's induced per-gene change against the real si | §4.1 external validation: r_chrom = +0.434 vs shuffled -0.028 +/- 0.19 (z = +2.40), gene-level residual r = -0 | MaxToki-1B forward passes + K562 and CD34 h5ad |
| `lineage_steer.py` | Causal steering of the lineage bistable switches (GATA1/SPI1, PAX5/PRDM1, RORC/FOXP3), reciprocity read through the model's own logits. | §4.3 '+4.211 [+3.892,+4.539] for GATA1/SPI1 and +5.565 for PAX5/PRDM1' | MaxToki forward passes + h5ad |
| `make_fig5.py` | Three-panel figure showing position is a linear direction orthogonal to top variance. | route paper Supp S2 figure | results/genome_position_geometry.json |
| `make_fig6.py` | Layer figure separating contextual hidden states from static weight tables. | route paper Supp S3 figure; main-paper Fig 3b data | results/maxtoki_layers.json |
| `make_figures.py` | Builds figures/fig1_decoding.pdf .. fig4_rotation.pdf for PAPER_chromosome_variable.md from results/*.json. | produces the route paper's Figures 1-4 (Fig 1c is the 0.880/0.720/0.506 panel) | results/*.json only |
| `make_graphical_abstract.py` | Composite graphical abstract (setup -> encoded -> causally used). | route paper front matter | results/*.json |
| `make_note_pdf.sh` | Builds NOTE_context_representation.pdf. | none in §4.1 | pandoc + xelatex |
| `make_paper_pdf.sh` | Builds PAPER_context_representation.pdf. | none in §4.1 | pandoc + xelatex |
| `make_pdf.sh` | Builds PAPER_chromosome_variable.pdf via pandoc + xelatex. | build script | pandoc, MacTeX at /Library/TeX/texbin |
| `maxtoki_layers.py` | Depth profile: decodes chromosome and position from the contextual hidden state at every layer, plus the two static tables as reference lines. | §4.1 'Where the variable lives': 0.453 W_E, 0.516 lm_head, decaying monotonically to 0.088 at L11 | MaxToki-217M forward passes over 600 cells (transformers venv) |
| `model_scale.py` | Matched 217M vs 1B comparison for chromosome and position on the identical 18,287 genes, plus random-projection dimension controls. | §4.1 scale: 1B 0.837 random / 0.703 group vs 217M 0.485 / 0.368; and Fig 3b 1B matched-width 0.813 -> 0.066 | both model safetensors + coordinates |
| `nonlinear_sweep.py` | Non-linear probe families (kNN, cosine, Nystroem RBF, MLP) for the same task. | §3.2 probe family | model tables + coordinates |
| `panel_sweep.py` | What makes a tissue carry chromosome-scale co-expression: sweeps panels matched at 3,500 cells (fetal gut 0.720 vs adult 0.497 vs CD34 0.080 vs TS kidney 0.08). | Supp S4 panel dependence | many h5ad panels |
| `position_improve.py` | Can the position read-out be improved: per-interval directions, per-chromosome probes, alternative regressors. | Supp S2 | MaxToki tables + coordinates |
| `purity_decomposition.py` | Shows cell-type heterogeneity specifically manufactures chromosome-scale co-expression (k=1 0.284 -> k=12 0.524 at fixed N, matched-difficulty control flat). | Supp S4 / mechanism discussion | h5ad panels with cell-type labels |
| `run_probe.py` | The supervised cross-validated ridge probe battery over gene_sets.py hypotheses; the instrument change that revived HOX (0.19 -> 0.884). | RESULTS.md §1 (methodology origin of §4.1) | all bases |
| `specificity_matched.py` | Confound-controlled rerun of specificity_mechanism.py (detection-rate matched). | Supp S4; supersedes specificity_mechanism.py | h5ad panels |
| `specificity_mechanism.py` | Which genes manufacture the chromosome signal: splits by cell-type specificity (Tau), matched on set size and chromosome composition. | Supp S4 | h5ad panels |
| `steer_algebra.py` | What kind of object the chromosome channel is: bidirectionality, additivity, cross-talk across all 22 chromosomes. | Supp S6 'algebra': 22 independent, signed, saturating lookups, not one continuous manifold | MaxToki-1B forward passes |
| `steer_classifier.py` | Trains the shared multi-head classifier on the pooled final cell embedding and runs the specificity test (does steering chromosome disturb unrelated heads). | Supp C.1 'chromosome is entangled with cell identity' (+0.541 disturbance in the 1B vs +0.208) | MaxToki forward passes + h5ad panels |
| `steer_coherence.py` | Are the 5-Mb domains that respond the biologically real ones (only 26 of 96 move the right way). | Supp C.1 caveat on steer_local | MaxToki forward passes |
| `steer_dosage.py` | Spatial profile of the steering response along the chromosome, separating a dosage mechanism from a co-regulated-program mechanism. | Supp C.1 'the causal response is flat along a chromosome, so fine position is readable but not used' | MaxToki-1B forward passes |
| `steer_layers2.py` | Depth of the causal effect with the chromosome direction rebuilt natively at each layer and pushes scaled to the local residual norm. | §4.1 'Steering works at the input embedding (+0.0801, 22/22) and is indistinguishable from zero by layer 5'; S | MaxToki-1B forward passes |
| `steer_local.py` | Steering toward 5-Mb local genomic domains rather than whole chromosomes, and whether the destination cell type over-expresses that domain. | Supp C.1 'the one positive mechanism result, 5-Mb local domains at +0.049 and +0.037, both p <= 0.0001' | MaxToki forward passes + h5ad panels |
| `steer_locality.py` | Decay curve of a local push: how far along the chromosome the effect reaches. | Supp C.1 characteristic scale | MaxToki forward passes |
| `steer_mechanism.py` | Does the steering destination match real biology: does chr-C steering send cells to the cell type that over-expresses chr-C genes (answer: no). | Supp C.1 'fail against expression enrichment in both (p = 0.92 and 0.115)' | MaxToki forward passes + h5ad |
| `steer_propagation.py` | Generalised split-half causal-propagation test for any categorical gene property; the independent reimplementation of genome_causal.py, with alpha sweep and multiple seeds. | §4.1 '132 of 132 chromosome x strength x seed combinations in the 1B (116/132 in the 217M)' and Fig 3c | MaxToki-217M and 1B forward passes |
| `steer_relative.py` | Tests a source-relative push (centroid(C) minus centroid(chrom of g)) against the global push. | Supp S6 operator check (did not beat global) | MaxToki forward passes |
| `steer_signed.py` | Is chromosome a signed dosage quantity or a category label: negative-alpha (suppression) sweep, coarse and fine. | Supp S6 'bidirectional but asymmetric' | MaxToki-1B forward passes |
| `steer_swap.py` | Assumption-free ceiling for the operator objection: literally swap genes rather than add a vector. | Supp S6 substitution ceiling | MaxToki forward passes |
| `steer_units.py` | What is +0.055 worth in the model's own units: baseline-relative, and against the real intervention it imitates (substituting actual chr-C genes). | Supp S6 magnitude: substitution ceiling +0.0082 (+0.37x baseline) vs the vector's +0.055 | MaxToki forward passes |
| `steer_where.py` | Where the classifier's predictions go when the chromosome variable is pushed (destination, not distance). | Supp C.1 'steering destinations do not replicate across models (3/22 modal agreement)' | MaxToki forward passes + trained heads |
| `synteny_transfer.py` | Label-free retrieval test: are two genes >=20 Mb apart on the same chromosome closer than a matched pair, per basis (AUROC). | §4.1 'Two limits': the ranking inverts, factorisation 0.565 vs both models 0.529 | model tables + LSA basis + coordinates |
| `table_grid.py` | Full 2x2 of input embed_tokens vs output lm_head x 217M vs 1B on identical genes, at native width and dimension-controlled. | §4.1 'strong in the vocabulary tables (0.516 output, 0.453 input)' | both model safetensors |
| `verify_probe.py` | Paired bootstrap over genes on the supervised-probe revivals; only HOX survives. | RESULTS.md §1 | all bases |

## Provenance

These files were collected from the working tree in which the study was run. Paths to large assets have been parameterised; nothing else about the analysis logic was changed.
